ext {
    androidBuild = [
            versionName: "1.0.2",
            versionCode: 2,
    ]
    GROUP = 'com.android.dimen'
    POM_URL = 'http://localhost:8081/repository/druid/'
    POM_PACKAGING = 'aar'
}
